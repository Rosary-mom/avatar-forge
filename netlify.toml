[build]
  command = "npm run build && pip3 install -r sims/requirements.txt && python3 sims/generate_cape.py && python3 sims/generate_hair.py"
  publish = "dist"

[build.environment]
  PYTHON_VERSION = "3.12"
